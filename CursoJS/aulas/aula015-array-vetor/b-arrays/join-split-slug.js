const array1 = [1, 2, 3, 4, 5, 6];

// Combinar
const combinado = array1.join('.');
console.log(combinado);

// Dividir
const frase = 'Mariline te espero na janta'
const cortado = frase.split(' ');

console.log(cortado);

// Slug
console.log(cortado.join('-'));