const numeros = [5, 3, 12, 17, 10, 99];

// Join junta os valores do array
console.log(numeros.join('-'));

const user = [
    'Anderson',
    29,
    2022,
    'Brasileiro'
];

console.log(user);
console.log(user.join(', '));
console.log(user.join('-'));