const numeros = [5, 3, 12, 17, 10, 99];

// Pop remove e retorna o último número do array
const numeroRetornado = numeros.pop();

console.log('Número Removido', numeroRetornado);
console.log(numeros);